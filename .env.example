# Runtime mode
NODE_ENV=production

# TCP engine port
PORT=5433

# Web UI port
WEB_PORT=8080

# Container bind address
HOST=0.0.0.0

# Compose project name (used for volume naming)
COMPOSE_PROJECT_NAME=sql-clone
